'use strict';

aboutController.$inject = [
  '$scope',
  '$state'
];

function aboutController (
  $scope,
  $state
) {
  const vm = this;

  function setup () {
  }

  setup();
}

module.exports = aboutController;
